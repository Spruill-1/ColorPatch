<Page
    x:Class="ColorPatch.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ColorPatch"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    mc:Ignorable="d">

    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
        <canvas:CanvasControl x:Name="CanvasControl" Draw="CanvasControl_Draw" Width="10" Height="10" CreateResources="CanvasControl_CreateResources"/>
        <Grid x:Name="grid" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition MinWidth="100" Width="Auto"/>
                <ColumnDefinition MinWidth="100" Width="Auto"/>
                <ColumnDefinition MinWidth="100" Width="Auto"/>
                <ColumnDefinition MinWidth="100" Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition MinHeight="100" Height="Auto"/>
                <RowDefinition MinHeight="100" Height="Auto"/>
            </Grid.RowDefinitions>
        </Grid>
    </StackPanel>
</Page>
